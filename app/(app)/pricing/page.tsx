// PAY-001 — Paywall / pricing. Phase D builds the real screen.
export const metadata = { title: "Choose a plan" };

export default function PricingPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-6">
      {/* PLACEHOLDER: pending copy from Hasnain */}
      <p className="text-muted-foreground">Pricing / paywall — Phase D</p>
    </main>
  );
}
